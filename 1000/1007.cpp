#include <iostream>

int main() {
    
    int a, b, c, d;

    std::cin >> a >> b >> c >> d;

    int result = (a * b) - (c * d);

    std::cout << "DIFERENCA = " << result << std::endl;

    return 0;
}